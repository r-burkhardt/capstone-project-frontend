import { ResolveAssetPathPipe } from './resolve-asset-path.pipe';

describe('ResolveAssetPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveAssetPathPipe();
    expect(pipe).toBeTruthy();
  });
});
