/**
 * A const value that stores all the configuration for communicating with the api.
 * The values are intended to be left empty here, and should be set at
 * ../src/environments/environment.ts file.
 */

export const apiConfig: any = {
  production: '',
  restApiUrl: '',
  accessToken: '',
  userId: '',
};
